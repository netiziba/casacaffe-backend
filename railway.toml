[build]
builder = "NIXPACKS"

[deploy]
# Runs before the new version goes live (has DB env vars). No Railway “Shell” UI needed.
preDeployCommand = "php artisan migrate --force"
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/up"
healthcheckTimeout = 300
